language: python

python:
  - "pypy"
  - "2.7"

install:
  - pip install coveralls 
  - pip install -r requirements.txt
  - python setup.py install

script:
  - python setup.py nosetests

after_success:
  - coveralls
